const swiperOptions = {
    spaceBetween: 30,
    effect: "fade",
    loop: true,
    autoplay: {
        delay: 3250,
        disableOnInteraction: false,
    }
};

var swiper = new Swiper(".swiper-container", swiperOptions);